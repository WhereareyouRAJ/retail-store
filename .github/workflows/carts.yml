name: 🚀 Carts CI

on:
  push:
    paths:
      - './src/carts/**'
    branches:
      - main

jobs:
  call-security-pipeline:
    uses: ./.github/workflows/reusable-build.yml@main
    with:
      image_name: "retail-carts"
      context: "./src/carts/"
    secrets: inherit